{
  "name": "@qpa/cli",
  "version": "0.1.2",
  "description": "pa cli",
  "private": true,
  "bin": {
    "qpa": "src/command/index.ts"
  },
  "scripts": {
    "clean": "rm -rf dist",
    "build": "bun run build",
    "test": "bun run --workspaces test",
    "publish-packages": "bun publish --workspaces --if-present"
  },
  "devDependencies": {
    "@commander-js/extra-typings": "^13.1.0",
    "@types/node": "^22.15.3",
    "chalk": "^5.3.0"
  },
  "keywords": [
    "qpa",
    "cli"
  ],
  "author": "chen56",
  "dependencies": {
    "commander": "^13.1.0",
    "@qpa/core": "^0.1.0"
  }
}
