// build阶段 配置
{
  "extends": ["./tsconfig.json","../../tsconfig.base.build.json"],

  // 继承根目录的共享配置
  "compilerOptions": {
    // build时必须设置rootDir,会影响编译结果的目录结构
    "rootDir": "./src",
  },
  // build时只需src即可
  "include": ["src"],
}