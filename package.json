{
    "name": "pa",
    "private": true,
    "version": "1.0.0",
    "workspaces": [
      "packages/*"
    ],
    "scripts": {
      "dev:cli": "npm run --workspace cli dev",
      "build": "npm run --workspaces --if-present build",
      "test": "npm run --workspaces test",
      "publish-packages": "npm publish --workspaces --if-present"
    },
    "devDependencies": {
      "chalk": "^5.3.0"
    }
  }