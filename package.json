{
  "name": "pa",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "clean": "rm -rf dist ",
    "build:all": "npm run --filter @pa/* build --if-present",

    "build": "echo 'root build ' ",
    "test": "bun test",
    "publish-packages": "bun publish --workspaces --if-present"
  },
  "devDependencies": {
  }
}